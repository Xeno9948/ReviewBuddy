{
    "build": {
        "rootDirectory": "review_buddy/nextjs_space"
    },
    "deploy": {
        "startCommand": "cp -r public .next/standalone/ && mkdir -p .next/standalone/.next && cp -r .next/static .next/standalone/.next/ && cp -r scripts .next/standalone/ && npx prisma db push && node .next/standalone/scripts/seed.js && HOSTNAME=0.0.0.0 node .next/standalone/server.js"
    }
}