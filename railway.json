{
    "build": {
        "rootDirectory": "review_buddy/nextjs_space"
    },
    "deploy": {
        "startCommand": "ls -R .next/standalone && npx prisma db push && HOSTNAME=0.0.0.0 node .next/standalone/server.js"
    }
}