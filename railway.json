{
    "build": {
        "rootDirectory": "review_buddy/nextjs_space"
    },
    "deploy": {
        "startCommand": "cp -r public .next/standalone/ && mkdir -p .next/standalone/.next && cp -r .next/static .next/standalone/.next/ && npx prisma db push && HOSTNAME=0.0.0.0 node .next/standalone/server.js"
    }
}